node_modules
dist
.git
.github
.husky
coverage
playwright-report
test-results
.playwright-mcp
*.log
.env
.env.local
.vscode
.idea
ship

# NOTE: `design/` is intentionally NOT ignored — index.tsx imports
# `design/language/theme.css` (the design tokens), so the build needs it.
