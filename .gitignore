node_modules/
dist/

# Git-diff offline-sync exports (present only when scaffolded without GitHub)
/git_diffs/

.env
.env.local
# Vanilla ship/ runtime secret (ORS key) — keep env.example.js, ignore env.js
ship/env.js
*.log
.DS_Store
coverage/
.nyc_output/

# Playwright — keep test specs and committed visual baselines (*-snapshots/),
# ignore generated reports and run artifacts.
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/
# Claude-in-Chrome / Playwright-MCP scratch output
/.playwright-mcp/
